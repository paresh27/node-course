// const fs = require("fs");
// const createNote = function (fileName, content) {
//   fs.accessSync(fileName, content);
// };

const getNotes = function () {
  return "Your notes...";
};

module.exports = getNotes;
